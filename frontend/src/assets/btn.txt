.btn {
  position: relative;
  overflow: hidden;
  padding: 12px 24px;
  border: none;
  background: #2563eb;
  color: white;
  cursor: pointer;
}

.btn::before {
  content: "";
  position: absolute;
  top: 0;
  left: -100%;
  width: 50%;
  height: 100%;
  background: linear-gradient(
    90deg,
    transparent,
    rgba(255, 255, 255, 0.6),
    transparent
  );
  transform: skewX(-25deg);
  transition: 0.6s;
}

.btn:hover::before {
  left: 150%;
}